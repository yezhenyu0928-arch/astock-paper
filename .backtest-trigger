# CI 回测触发戳。修改本文件内容并 push,即触发 .github/workflows/backtest.yml 在 CI 全量库上
# 跑指定策略的五关回测+蒙特卡洛,报告 commit 回 main。见 docs/OPTIMIZE_V4.md 四点五节。
trigger: 2026-07-11T06:00 v4-final-clean-run (ROOT CAUSE CONFIRMED: stock_annual table was completely empty — 0 rows, 0 codes — in current CI DB cache. backtest.yml's "ensure full DB" gate only checked daily_bar coverage, never stock_annual, so the empty table kept getting perpetuated via cache/save across repeated CI triggers. Fixed the gate to also check+targeted-repair stock_annual coverage. All diagnostic code removed from s1_dividend_v3.py (file now byte-identical to fd7cadc). This run should show s1_dividend@v3 trading for real.)
