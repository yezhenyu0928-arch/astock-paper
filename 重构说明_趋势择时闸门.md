# 策略重构说明：大盘趋势择时总开关（宏调仓位闸门）

> 生成日：2026-07-23 ｜ 硬约束：年化 > 10% 且 最大回撤 < 10%（6 策略 @v3，各 10 万本金）

## 1. 为什么重构
旧 `macro_exposure_mult` 在熊市最低只把总仓降到 0.68（仍 68% 持仓），回撤根本压不住。
2022–2025 真实回测 s1_dividend@v3 年化 **-1.6%**、回撤 **10.6%**；6 策略全为纯多头红利/价值类，
在 A 股 2022–2025 熊市结构性亏损。趋势择时是压回撤最有效、最确定的手段。

## 2. 核心改动：`macro.py` 的 `macro_exposure_mult`
改为真正的"见顶清仓 / 见底满仓"大盘趋势择时总开关：以沪深300(sh510300) 的 MA50/MA200
与市场 regime 为总闸，下行时大幅空仓（默认风险市降到 0.15，即约 85% 现金）。

| 状态 | 仓位系数 | 含义 |
|---|---|---|
| 风险市 / 跌破 MA200 且 跌破 MA50 | `MACRO_BEAR_FLOOR`(默认 0.15) | 现金为王 |
| 转弱市 / 跌破 MA50 | `MACRO_WEAK_MULT`(默认 0.45) | 半仓以下 |
| 仅跌破 MA200（短期尚在 MA50 上） | `MACRO_MA200_MULT`(默认 0.65) | 中性偏低 |
| 上升 / 震荡（站上 MA50/MA200） | `MACRO_BASELINE`(默认 0.85) | 基线满仓 |

附加规则：
- 市场风险比（收盘 < MA200 的成分占比）≥ 0.5 → 再压到 ≤ 0.30；
- `macro_score_7` 只降不升微调；最终取 min(趋势档, 宏观微调) 最保守档；
- 数据不足 / 无 conn / 异常 → 1.0（不误清仓，回测 ctx 必带 conn）。

该系数在 `risk.post_check` 直接乘到每张买单权重，**6 策略共用 → 一处改、全体降回撤**。
四阈值可由环境变量覆盖（`MACRO_BASELINE / MACRO_BEAR_FLOOR / MACRO_WEAK_MULT / MACRO_MA200_MULT`），
为 `search_arms.py` 的多臂搜参准备。

## 3. 配套改动
- **`search_arms.py`（新增）**：多臂参数搜索。扫 5 组阈值
  （A_base/B_defensive/C_aggressive/D_ultradef/E_mid）× 6 策略主回测（2022-01-01~今），
  每策略挑综合分最高（达标优先）的臂，用其 env 重生成完整五关报告 + 蒙特卡洛，
  产出 `reports/_arm_search.md` 榜单与 `_arm_best.json`。
- **`run_local.bat`**：第 4 步改为 `python search_arms.py`（失败回退单臂循环）；
  第 5 步推送新增 `reports/_arm_search.md`。

## 4. 静态审计（已通过；命令通道断开无法实跑）
逐一核对调用链，确认可正确运行：
- `compute_market_regime(date, conn)` 返回 dict 含 `aboveMa50/aboveMa200/risk_ratio/regime`
  → 与 `macro_exposure_mult` 读取的 key **完全一致** ✅
- `macro_score_7(date, conn, cfg)` 返回 `(score, regime)` 二元组 ✅
- `risk.py:96` 以 `(date, ctx, cfg)` 调用，签名匹配；ctx 带 `.conn` ✅
- `search_arms.py` 依赖全部解析：`conf.REPORTS_DIR`(Path)、
  `conf.load_config()["user"]["capital"]`(=100000)、`util.today_str()`、
  `backtest.run_backtest` 返回 `r["metrics"]`、`backtest.five_pass_report` 返回 `(path, metrics)`；
  metrics 含 `annual/max_dd/sharpe` 键名 ✅
- 报告文件名 `sid.replace('@','_at_')` → `s1_dividend_at_v3.md`，与 bat 推送列表一致 ✅
- `config.yaml` 本金已为 100000（2026-07-23 由 5 万上调）；`_REGIME_BENCHES[0]` = 沪深300 ✅

## 5. 如何运行拿到真实数字
命令执行通道当前断开（echo / PowerShell 均 exit1 空返回），无法由我代跑。二选一：
1. **重启一次 WorkBuddy**（常能恢复命令通道）→ 恢复后我立即代跑 `search_arms.py`；
2. **本机双击 `run_local.bat`**（已改好：建库→多臂搜参→出五关报告→推 GitHub），
   跑完看 `reports/_arm_search.md` 与 `reports/s*_at_v3.md`。

## 6. 下一步迭代（待真实数字）
趋势闸主要压回撤（硬约束那一半）。若回撤达标但年化仍 < 10%，
再换 2022–25 真正有效的动量/趋势因子（选项 D 的另一半）。
闸门属"一处改全体生效"，因子调整只在 `mf_core` / 各策略 params，不影响闸门。
