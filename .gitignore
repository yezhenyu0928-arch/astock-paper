# 历史数据库:大二进制,不进git(Actions 用 cache 持久化,本地 backfill.py 重建)
db/*.sqlite
db/*.sqlite-wal
db/*.sqlite-shm
db/*.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 系统
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/

# 注:state/ reports/ docs/ 需提交(daily.yml 靠 git 持久化账户净值与看板)

# WorkBuddy 项目状态目录(automations/memory/ci 文件), 非仓库内容, 不进 git
.workbuddy/

# 调参/回测临时脚本与日志(下划线前缀), 本地探索用, 不进 git
_*.py
_*.log
_*.out
_*.sh
_cache/
_ci_r2/
reports_cloud/

# 回测子进程日志(本地探索用)
bt_*.err
bt_*.json
backfill_fund.log
cdp_*.mjs
extract_rules.py

# 本地种子库构建/抓取产物(不入库; 种子库走 db-seed 分支, 云端 prep 整库还原)
db_seed*
seed.db
db_seed_tmp.sqlite
fetch_profit_q.log
