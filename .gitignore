# 历史数据库:大二进制,不进git(Actions 用 cache 持久化,本地 backfill.py 重建)
db/*.sqlite
db/*.sqlite-wal
db/*.sqlite-shm
db/*.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 系统
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/

# 注:state/ reports/ docs/ 需提交(daily.yml 靠 git 持久化账户净值与看板)

# WorkBuddy 项目状态目录(automations/memory/ci 文件), 非仓库内容, 不进 git
.workbuddy/
